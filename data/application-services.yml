---
app srvs:
  color: 7
  pages:
    Overview:
      url_rules:
        - /application-services/overview
    All pages:
      url_rules:
        - /application-services/**
    Learning Resources:
      url_rules:
        - /application-services/learning-resources
    Quickstarts:
      url_rules:
        - /application-services/**?quickstarts=*parameter*
    Subscriptions:
      url_rules:
        - /application-services/subscriptions
