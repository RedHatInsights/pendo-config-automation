---
app srvs:
  color: 1
  pages:
    Connectors:
      url_rules:
        - /application-services/connectors
        - /application-services/connectors/**
    Connectors - Create connector:
      url_rules:
        - /application-services/create-connector
